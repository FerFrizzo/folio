# Drop signing keys + service-account JSON in this directory. Everything is
# ignored except this file itself, so accidentally checked-in secrets don't
# end up in git history.
*
!.gitignore
!README.md
